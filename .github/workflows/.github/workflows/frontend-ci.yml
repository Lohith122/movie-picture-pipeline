name: Frontend CI/CD Pipeline

on:
  push:
    branches: [ main ]
    paths:
      - 'starter/frontend/**'
      - '.github/workflows/frontend-ci.yml'

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install and Test
        run: |
          cd starter/frontend
          npm install
          npm test -- --watchAll=false || true

  build-and-push:
    needs: lint-and-test
    runs-on: ubuntu-latest
    steps:
      - actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: "AKIAT2DQMVN5XSMDBYRP"
          aws-secret-access-key: "q+uriF4PJY15NChpgfD9iCLbNKMRzahPx+ZIf"
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, Tag, and Push Frontend Image
        env:
          ECR_REGISTRY: 262228716411.dkr.ecr.us-east-1.amazonaws.com
          IMAGE_TAG: latest
        run: |
          docker build -t $ECR_REGISTRY/frontend:$IMAGE_TAG ./starter/frontend
          docker push $ECR_REGISTRY/frontend:$IMAGE_TAG
          docker build -t $ECR_REGISTRY/frontend:${{ github.sha }} ./starter/frontend
          docker push $ECR_REGISTRY/frontend:${{ github.sha }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: "AKIAT2DQMVN5XSMDBYRP"
          aws-secret-access-key: "q+uriF4PJY15NChpgfD9iCLbNKMRzahPx+ZIf"
          aws-region: us-east-1

      - name: Update Kubeconfig
        run: |
          aws eks update-kubeconfig --name cluster --region us-east-1

      - name: Deploy Frontend Manifests
        run: |
          kubectl apply -k starter/frontend/k8s/ || kubectl apply -f starter/frontend/k8s/
          kubectl rollout restart deployment frontend 2>/dev/null || true